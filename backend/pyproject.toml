[project]
name = "super-pong-backend"
version = "0.1.0"
description = "Super Pong beer pong tracker backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "sqlmodel",
    "python-multipart",
]

[tool.uv]
dev-dependencies = [
    "pytest",
    "httpx",
]
