[project]
name = "elbow-tracking"
version = "0.1.0"
description = "Elbow violation tracking for beer pong using MediaPipe"
requires-python = ">=3.12"
dependencies = [
    "opencv-python",
    "mediapipe",
]
